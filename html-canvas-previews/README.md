# html-canvas previews

2D previews generated by ClawBot's html-canvas tool as a planning step before building in Blender. Each preview uses a grid coordinate system that maps directly to Blender's local XY space, so approved designs transfer deterministically into 3D meshes without reinterpretation.
