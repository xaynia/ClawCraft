# Workflow Notes

Personal observations about the AI-assisted 3D art process. What the pipeline enables, what it limits, what surprises me. Feeds the report workflow comparison section.

Observations were captured in `render_log.json` (via the `workflow_observation` field on each render) and in `lessons-learned.md` rather than this file.

---

